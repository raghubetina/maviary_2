<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render object: @contact, partial: "card", as: :contact %>
  </div>
</div>

  <!-- Show Page Customization starts -->
<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "contacts/contact_circles_contacts" %>
  </div>
</div>

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "contacts/contact_circles" %>
  </div>
</div>
  <!-- Show Page Customization end -->
